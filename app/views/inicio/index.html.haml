-# Landing page del sitio con mapa de perfiles

-# Container con altura definida para el mapa
#container-mapa.d-flex.flex-column
  #mapa{ data: {  geojson: perfiles_path(format: :geojson),
                  seleccion_url: seleccion_path,
                  info_title: t('.title'),
                  info_text: t('.text', perfiles: @perfiles, publicos: @publicos, series: @series),
                  center: @map[:center],
                  zoom: @map[:zoom],
                  icon_url: asset_path('leaflet/dist/images/marker-icon.png'),
                  icon_retina_url: asset_path('leaflet/dist/images/marker-icon-2x.png'),
                  shadow_url: asset_path('leaflet/dist/images/marker-shadow.png') } }
